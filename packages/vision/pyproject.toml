[project]
name = "aegis-vision"
version = "0.1.0"
description = "AEGIS 순수 비전 로직 — 호모그래피 · 접지점 · 구역 · 거리 · 자세 (하드웨어 의존 0)"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aegis_vision"]
