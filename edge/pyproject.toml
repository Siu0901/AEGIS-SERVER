[project]
name = "aegis-edge"
version = "0.1.0"
description = "AEGIS 실물 Jetson 러너 — M9 까지 비워 둔다"
requires-python = ">=3.13"
dependencies = [
    "aegis-contracts",
]

# 휠로 빌드하지 않는다. 실물 젯슨에서 레포 루트를 기준으로 실행한다.
[tool.uv]
package = false
