# AEGIS

중소 제조현장(상시근로자 5~50인)의 CCTV 영상을 AI로 실시간 분석해 안전 위반을 감지하고,
현장 스피커로 즉시 경고 방송을 내보내고, **실제로 시정되었는지까지 자동으로 확인**하는 시스템.

핵심 지표는 **「방송 후 시정률」**이며, 이것이 이 프로젝트의 유일한 차별점이다.
상용 제품은 "감지 → 알림"에서 끝나므로 이 숫자가 존재할 수 없다.
설계 판단이 갈릴 때는 **이 지표의 신뢰성을 지키는 쪽**을 택한다.

팀 AIM (이시우 · 손서원) · 2026 포스코DX AI Youth Challenge

---

## 이 레포의 범위

**기능명세서의 P0 + P1 기능 전량을 실제로 동작하게 구현한다.**
프로토타입이나 데모용 껍데기가 아니다. 모든 기능이 끝까지 작동해야 한다.

| 범위 안 | 범위 밖 |
|---|---|
| 서버 전체 (상태머신·DB·REST·WS·스트리밍·클립·경고·AI) | Jetson 실물 이식 |
| 프론트 전체 (7개 화면, 디자인 시안 일치) | ESP32 펌웨어 |
| `packages/vision` 순수 로직 (호모그래피·구역·거리·자세) | 모델 학습·가중치·임계값 튜닝 |
| `sim/` 시뮬레이터 (가짜 엣지 · 가짜 MCU) | 감지 품질 판단 |
| `deploy/` 가짜 RTSP 환경 | |

**엣지와 MCU는 시뮬레이터로 대체한다.** 서버 입장에서 시뮬레이터는 실물과 구분되지 않는다.
실물 연동은 서버·프론트가 전부 완성된 뒤 마지막에 교체하는 방식으로 진행한다.
따라서 **개발 중에 실제 모델이나 하드웨어를 붙이려 하지 마라.**

---

## 아키텍처 3계층

| 계층 | 장비 | 역할 | 원칙 |
|---|---|---|---|
| Tier 0 · 엣지 | Jetson Orin Nano | 감지·추적·좌표·거리·자세 계산 | **판단하지 않는다.** 후보만 올린다 |
| Tier 1 · 서버 | 노트북 (현장 내) | 확정·경고·시정판정·DB·클립 | 인터넷 없이도 완결된다 |
| Tier 2 · 클라우드 | Google Gemini API | 임베딩·LLM 분석 | 죽어도 안전 기능은 무영향 |

영상은 듀얼 스트림으로 분리된다. **1080p 메인은 서버**(라이브·7일 녹화·클립 원본),
**640p 서브는 엣지**(추론). 엣지는 영상을 중계하지 않고 메타데이터만 서버로 보낸다.

---

## 절대 규칙

1. **시간을 직접 읽지 않는다.** `datetime.now()` / `time.time()` 호출 금지.
   `Clock` 프로토콜을 주입받는다. 테스트가 3초·10초·15초·30초·300초 타이머를
   순간이동시켜야 하므로 이 규칙이 깨지면 검증 자체가 불가능해진다.
2. **`server/domain` 과 `packages/vision` 에 I/O를 넣지 않는다.**
   DB·네트워크·파일·시간 전부 금지. 순수 함수와 순수 상태 전이만.
3. **엣지는 판단하지 않는다.** 규칙에 걸리면 후보(candidate)만 올리고,
   확정·경고·시정판정은 전부 서버가 한다. `sim/edge_sim` 도 이 규칙을 지킨다.
4. **거리 수치는 항상 호모그래피 값을 쓴다.** 단안 뎁스는 절대거리가 부정확하므로
   앞뒤 분리 판별과 깊이 분산 확인에만 사용한다.
5. **스키마는 `packages/contracts` 에만 정의한다.** 다른 곳에서 재정의 금지.
   `sim/` 도 반드시 이 스키마를 import 해서 메시지를 만든다.
6. **모델 경로·임계값·튜닝 파라미터를 코드에 하드코딩하지 않는다.**
   엣지는 `edge/config.yaml`, 서버 정책값은 DB `policies` 테이블.
7. **최상위 디렉토리를 새로 만들지 않는다.** 기존 것 중에서 고르고, 애매하면 물어본다.
   (`utils/`, `common/`, `lib/`, `helpers/` 같은 걸 임의로 만들지 말 것)
8. **`docs/` 가 SSOT다.** 코드와 충돌하면 명세서가 맞다.
   명세서를 고쳐야 한다고 판단되면 **코드를 바꾸기 전에 사람에게 보고**한다.
9. **기능을 임의로 축소하지 않는다.** "일단 간단히", "나중에 확장 가능하게"로
   명세된 기능을 생략하지 마라. 못 하겠으면 생략하지 말고 보고해라.
10. **제로샷/오픈보캐블러리 감지는 존재하지 않는다.** 감지 클래스는 `person`·`vehicle`
    2종 고정이다. 「위험요소 자연어 등록」 기능은 채택되지 않았으므로 화면·API·DB
    어디에도 만들지 마라. 디자인 시안에 남아 있어도 무시한다. (기능명세서 부록 A-1)
11. **디자인 시안보다 명세서가 우선한다.** `docs/front_design.pdf` 는 건설현장 시절에
    작성되어 라벨이 낡았다. 레이아웃·정보구조·색상은 따르되 **텍스트와 도메인 용어는
    명세서를 따른다.** (기능명세서 부록 B)

---

## 자주 틀리는 것 (명세서에 명시되어 있음)

- **안전모에는 별도 bbox가 없다.** 1단계 감지는 `person` / `vehicle` 2클래스뿐이고,
  안전모는 사람 크롭을 2단계 분류 모델이 판정한다. 화면에는 사람 박스의 색으로 표현한다.
- **`helmet` 값은 `on` / `off` 두 개뿐이다.** `unknown` 클래스는 없다.
  크기·신뢰도 게이트를 통과 못 하면 직전 값을 유지하거나 필드를 생략하며,
  이때 확정·해소 타이머는 **리셋이 아니라 동결**한다.
- **클립은 확정 즉시 추출하지 않는다.** 확정 순간에는 사후 구간이 아직 녹화되지 않았다.
  `confirmed_at + clip_post_roll_s + margin` 시점에 예약 실행한다. 키프레임만 즉시.
- **`expired`(추적 단절로 판정 불가) 이벤트는 시정률 분모·분자 모두에서 제외**하고
  `undetermined_rate`로 따로 집계한다. 두 숫자는 항상 병기한다.
- **재결합은 이벤트를 살리는 처리이지 시정을 인정하는 처리가 아니다.**
  재결합 후 위반이 사라져 보여도 해소 타이머를 0부터 다시 채워야 한다.
- **좌표는 두 종류뿐이다.** 접미사 없으면 정규화 픽셀(0.0~1.0), `_m` 접미사면 지면 실좌표(미터).
- **오버레이는 도착 즉시 그리지 않는다.** 영상 경로와 좌표 경로의 지연이 다르므로
  `ts` 기준 지연 버퍼에 담았다가 재생 중인 프레임 시각에 맞춰 그린다.
- **경고 방송은 TTS가 아니다.** 위반 유형별 사전 녹음 wav를 재생한다(생성 지연 제거).
- **이상 탐지는 경고 방송을 발동하지 않는다.** 대시보드 '주의' 알림으로만 표시한다.
- **설정 화면에 「위험요소 등록 · 자연어」 패널은 없다.** 시안에 있어도 만들지 않는다.
- **"굴착기"·"굴착 구역"은 존재하지 않는다.** 지게차 / 지게차 통행로다.
  시안의 건설현장 용어는 전부 제조현장 용어로 바꿔서 구현한다.
- **엣지 메시지가 스키마 검증에 실패해도 조용히 버리지 않는다.** 원본 페이로드와
  오류를 로깅하고, `edge.msg_rejected_total` 카운터를 올리고, `GET /system/status` 와
  대시보드에 노출한다(FN-SYS-06). 감지된 위반이 소리 없이 사라지는 것은 오탐보다 위험하다.
- **카메라 서브 스트림은 640×360(16:9)이다.** 메인 1920×1080과 **화면비가 같아야**
  정규화 좌표가 그대로 대응된다. 640×640 같은 정사각 설정은 쓰지 않는다.
  정사각화가 필요하면 모델 입력 단계의 레터박스(640×384 rect)에서 처리한다.

---

## 핵심 수치

명세서를 열지 않고도 알고 있어야 하는 값들이다.

| 구분 | 값 |
|---|---|
| 카메라 메인 | 1920×1080 · H.264 · 15fps → 서버 (라이브·녹화·클립) |
| 카메라 서브 | 640×360 · H.264 · 15fps → 엣지 (추론) · **메인과 16:9 동일** |
| 모델 입력 | 640×384 rect letterbox (정사각 아님) |
| 엣지 처리 | 카메라당 8fps 이상 |
| 확정 지속 | 3초 (`confirm_duration_s`) |
| 해소 지속 | 10초 (`resolve_duration_s`) |
| 재경고 쿨다운 | 30초 (`cooldown_s`) |
| 트랙 소실 유예 | 15초 (`track_lost_grace_s`) |
| 재결합 시간 창 | 10초 (`reassoc_window_s`) |
| 재결합 반경 | `1.5 m/s × Δt`, 상한 5m |
| 시정 인정 창 | 300초 (`resolve_window_s`) |
| 지게차 위험 반경 | 3.0m |
| 분류 최소 크롭 | 64px (`cls_min_crop_px`) — 서브 기준 프레임 높이 약 18% |
| 클립 전/후 구간 | 각 10초 |
| 녹화 보존 | 7일 순환 |
| 확정 → 경고 | 1초 이내 |
| 오버레이 정합 오차 | ±100ms |

정책값은 전부 DB `policies` 테이블에서 읽는다. 위 값은 기본값이며 코드에 하드코딩하지 않는다.

---

## 디렉토리

```
docs/                명세서 3종 + INDEX.md  (SSOT)
packages/contracts/  Pydantic 스키마 — 서버·시뮬레이터·엣지가 공유
packages/vision/     순수 로직: 호모그래피·접지점·구역·거리·자세  (하드웨어 의존 0)
server/domain/       상태머신·지표  (I/O 없음)
server/app/          FastAPI · REST · WebSocket
server/infra/        db · stream · clip · audio · mqtt
server/ai/           LangGraph · Gemini 어댑터
front/               React + Vite 관제 화면 (7개)
sim/edge_sim/        가짜 젯슨 — frame·candidate·track_lost·heartbeat 생성
sim/mcu_sim/         가짜 ESP32 — aegis/alert 구독, device/status 발행
sim/cases/           시나리오 파일
deploy/              mediamtx.yml · fake_cams.sh · mosquitto.conf
edge/                실물 젯슨 러너 (M9에서 작업, 그 전까지 비워둠)
models/              가중치·엔진 (git 제외, 사람이 관리)
assets/              경고 음원 wav · 규정 매핑 · 사고사례 시드
scripts/             유틸
media/               런타임 저장소 — 녹화·클립·키프레임 (git 제외)
```

---

## 명령어

```
make verify      lint + typecheck + pytest + 마이그레이션 + 스모크 + 프론트 빌드
make dev         docker-compose + 서버 + 프론트
make cams        가짜 RTSP 2채널 송출 (mediamtx + ffmpeg)
make sim CASE=<이름>       가짜 엣지 실행
make mcu         가짜 ESP32 실행
make migrate     alembic upgrade head
make types       contracts → front TypeScript 타입 생성
```

작업을 마쳤다고 판단하기 전에 반드시 `make verify` 가 통과해야 한다.

---

## Git

- 커밋 단위는 **FN-ID 하나**를 원칙으로 한다. 여러 FN을 한 커밋에 섞지 않는다.
- 커밋 메시지 형식: `<타입>(<영역>): <FN-ID> <내용>`
  ```
  feat(evt): FN-EVT-02 이벤트 확정 판정 구현
  feat(front): FN-UI-03 이벤트 목록·상세 화면
  fix(domain): FN-EVT-03 해소 타이머가 게이팅 중 리셋되던 문제
  docs: INDEX 진척 갱신
  chore: contracts 타입 재생성
  ```
  타입: feat / fix / refactor / test / docs / chore
  영역: contracts vision domain app infra ai front sim deploy
- **커밋은 해도 되지만 `git push` 는 하지 마라.** 푸시는 사람이 한다.
- `git add .` 대신 변경한 파일을 명시해서 스테이징한다.
- `media/`, `models/weights/`, `models/engines/`, `.env` 는 절대 커밋하지 않는다.
- 커밋 전 `make verify` 가 통과해야 한다.

---

## 문서 지도

작업 전에 해당 절만 읽는다. 명세서 전체를 읽지 마라.

| 내용 | 위치 |
|---|---|
| 감지 기능 FN-DET-01~13 | `docs/AEGIS_기능명세서.md` §4.1 |
| 이벤트 처리 FN-EVT-01~07 (상태 전이표) | §4.2 |
| 경고 FN-ALM-01~05 | §4.3 |
| 기록·영상 FN-REC-01~05 | §4.4 |
| 지능 기능 FN-AI-01~10 | §4.5 |
| 관제 화면 FN-UI-01~07 (오버레이 규칙·시간 정합) | §4.6 |
| 설정 FN-CFG · 시스템 FN-SYS · 지표 산출 정의 | §4.7 · §4.8 |
| 데이터 모델 (테이블·컬럼) | §6 |
| 엣지→서버 메시지 (`frame` `candidate` `track_lost` `heartbeat`) | `docs/AEGIS_API명세서.md` §2 |
| MQTT (ESP32) | §3 |
| REST API · 정책 키 목록 | §4 |
| 서버→대시보드 WebSocket · 오버레이 정합 | §5 |
| 필드 산출법 (접지점·자세·시정률·이상점수) | §6 |
| 배경·근거·비용·시연 계획 | `docs/AEGIS_구체화_계획안_최종.md` |
| FN-ID ↔ 코드 위치 ↔ 테스트 매핑 · 진척 | `docs/INDEX.md` |
| 화면 디자인 시안 (레이아웃만 참조, 라벨은 명세서 우선) | `docs/front_design.pdf` |
| 미채택 기능 · 시안 불일치 대조표 | `docs/AEGIS_기능명세서.md` 부록 A · B |