"""REC — 녹화 컴포넌트 (API명세서 §4.7 · 기능명세서 §4.4).

메인 스트림 녹화와 구간 추출만 담당하는 **독립 컴포넌트**다.
개발 중에는 서버와 같은 기계에서 돌지만 M9 에 엣지(Jetson) NVMe SSD 로 옮긴다.

**서버와 파일시스템을 공유한다고 가정하지 않는다.** 같은 기계에 있더라도 서버는
녹화 파일에 파일 경로로 접근하지 않고 HTTP API(§4.7)만 쓴다. 옮길 때 고쳐야 하는
값이 `RECORDER_BASE` 하나뿐이어야 하기 때문이다.
"""
