"""시뮬레이터 — 가짜 젯슨(`edge_sim`) · 가짜 ESP32(`mcu_sim`).

**서버 입장에서 시뮬레이터는 실물과 구분되지 않는다.** 그래서 메시지는 반드시
`aegis_contracts` 스키마로 만들고, **엣지가 판단하지 않는다는 규칙도 그대로 지킨다**
— 후보(`candidate`)만 올리고 확정·경고·시정판정은 서버 몫이다.
"""
